# HS1210D_20260216 – Comprehension Summary

**Location:** `ScaniaRPG2JavaRevisedDesign/HS1210D_20260216/`  
**Contents:** Display file **HS1210D** – DDS AST and DDS source for the warranty claims administration screen.

---

## 1. What’s in This Delivery

| File | Description |
|------|-------------|
| **HS1210D-ast.json** | **DDS AST** (version `dds-ast/1.0`), generated by PKS-DDS-FrontEnd 9.2.3.2. Contains structured metadata for the display file: record formats, fields, types, and UI contracts. |
| **HS1210D.DSPF** | **Raw DDS source** (729 lines). The actual screen layout: record formats, field positions, keywords (SFL, SFLCTL, DSPATR, COLOR, CFnn), and literals. |

---

## 2. DDS AST (HS1210D-ast.json) – Structure

- **unit:** `qsys:HSSRC/QDDSSRC/HS1210D`, member type **DSPF**, library HSSRC.
- **files[]:** HS1210D and reference file HSREF.
- **types:** Character types (t.char.1, t.char.3, …, t.char.210) used by field definitions.
- **uiContracts.displayFiles:** One display file entry:
  - **symbolId:** `sym.dspf.HS1210D`
  - **name:** HS1210D
  - **recordFormats:** List of record formats, each with **name**, **keywords**, and **fields**.

### Record formats (main ones)

| Record | Purpose | Notable fields (from AST) |
|--------|--------|---------------------------|
| **HS1210S1** | Subfile (SFL) – one row per claim | SUB000, SUB030, SUB040, SUB02O, SUB160, MARK1, SUB050, SUB010, SUB020, SUB060, SUB140, SUB150, SUBDMC, SUB170, SUBANZ, SUBCOL |
| **HS1210C1** | Subfile control (SFLCTL) for HS1210S1 | REC1, PAG1, FILTERA, F16TXT, F17TXT, FILTAG, XXDAT, SUCHEN, FILTER, FILART; SFLSIZ(0011), SFLPAG(0010); function keys CF03–CF19 |
| **HS1210S2** | Second subfile | SUB010, SUB02O, … |
| (others) | Further formats in DSPF | See HS1210D.DSPF |

### Field metadata (in AST)

- **name:** DDS field name (e.g. SUB050, MARK1).
- **typeId:** e.g. t.char.8, t.char.2.
- **row / column:** Position (when present; some are 0,0).
- **keywords:** e.g. DSPATR(ND), COLOR(RED), EDTWRD(…).

---

## 3. DDS Source (HS1210D.DSPF) – Highlights

- **Device:** DSPSIZ(24 80), REF(HSREF), PRINT.
- **Subfile:** HS1210S1 (SFL), HS1210C1 (SFLCTL) with SFLSIZ(0011), SFLPAG(0010).
- **Function keys:** CF03 Verlassen, CF05 Aktualisieren, CF06 Erstellen, CF09 Listenanfang, CF11 Ansicht, CF12 Zurück, CF16 Sortierung, CF17 Sortierung Auf/Ab, CF19 Filter setzen.
- **Screen titles/labels:** e.g. “SDPS Verwaltung Garantieanträge”, “Auswahl”, “Claim”, “Rechnungs-”, “Chassis”, “K U N D E”, column headers for subfile (Claim Nr., Rechnungs-Nr., Datum, Chassis Nr., Kunde, Name, Dm, Status, Fe/An).
- **Fields:** Subfile fields (SUB000, SUB010, SUB020, …), control fields (FILTERA, F16TXT, SUCHEN, FILTER, FILART, FILTAG, XXDAT, etc.), REC1, PAG1.
- **Colors:** COLOR(RED), COLOR(BLU), COLOR(WHT), COLOR(GRN), COLOR(YLW).

This aligns with the HS1210 claim list / selection screen (warranty claims administration).

---

## 4. How This Fits the Pipeline

1. **Enrichment with DDS source**  
   Use this folder as **`--ddsDir`** so the enricher can load the raw DDS for HS1210D and attach it as **`ddsSource`** to context packages that reference `sym.dspf.HS1210D`:

   ```bash
   python3 enrich_context_with_display_files.py \
     --astDir JSON_ast/JSON_20260211 \
     --contextDir context_index \
     --ddsDir HS1210D_20260216 \
     --attachUnitDspf
   ```

   The script looks for a file named **HS1210D** with extension in `.mbr`, `.txt`, `.dspf`, or none; **HS1210D.DSPF** matches (on case-insensitive filesystems). If your OS is case-sensitive and it fails, rename to `HS1210D.dspf` or add `.DSPF` in the script.

2. **RPG AST**  
   The **RPG** AST (e.g. `HS1210-ast.json`) still defines which procedures reference HS1210D (via `sym.dspf.HS1210D` in edges/sem). The enricher uses the RPG AST to attach display files to context; the DDS content comes from **this** folder.

3. **DDS AST (HS1210D-ast.json)**  
   Use this for UI generation or tooling that needs **record formats and field lists** (names, types, positions) rather than raw DDS text. The migration prompt currently uses **ddsSource** (raw DDS); a future step could also pass `uiContracts` or a summary from this DDS AST.

---

## 5. Quick Reference

| Item | Value |
|------|--------|
| Display file | HS1210D |
| Symbol ID | sym.dspf.HS1210D |
| Record formats (main) | HS1210S1 (subfile), HS1210C1 (control), HS1210S2, … |
| DDS AST version | dds-ast/1.0 |
| Generator | PKS-DDS-FrontEnd 9.2.3.2 |
| Enrichment `--ddsDir` | `HS1210D_20260216` (this folder) |

---

## 6. Next Steps

1. Run enrichment with `--ddsDir HS1210D_20260216` so context packages get **ddsSource** for HS1210D.
2. Re-run Pure Java migration for nodes that use HS1210D (e.g. HS1210_n404) so the prompt includes the DDS content.
3. Optionally use **HS1210D-ast.json** `uiContracts.displayFiles[0]` (recordFormats + fields) for structured UI generation or validation.
